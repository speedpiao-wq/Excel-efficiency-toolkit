# 安全说明

## 使用边界

- 当前版本面向 Windows 桌面版 Excel；Excel 网页版不能运行 VBA `.xlam`。
- 加载项只处理用户当前选择的文本框，不上传工作簿、文本内容或个人数据。
- 用户偏好通过 VBA `GetSetting` / `SaveSetting` 保存在当前 Windows 用户配置中。
- 使用已完成的加载项不需要开启“信任对 VBA 工程对象模型的访问”。该权限只可能在构建源码时需要。

## 宏安全

不要为了安装本项目而开启“启用所有宏”。通过互联网下载的 `.xlam` 可能带有 Windows Internet Zone 标记。只有在确认文件来自本项目官方 Release 且校验值匹配时，才考虑使用文件属性中的“解除锁定”。

面向更大规模公开分发前，项目应使用可信代码签名证书，并在 Release 中同时发布 SHA-256 校验值。

## 报告安全问题

请不要在公开 Issue 中上传包含客户资料、财务数据、人事信息或其他敏感内容的工作簿。公开仓库建立后，请通过维护者指定的私密渠道报告安全问题。
